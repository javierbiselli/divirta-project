import React from "react";
import styles from "./footer.module.css";

const Footer = () => {
  return <footer className={styles.footerContainer}>footer in progress</footer>;
};

export default Footer;
