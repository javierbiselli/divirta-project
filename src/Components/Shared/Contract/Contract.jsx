import styles from "./contract.module.css";

const Contract = () => {
  return (
    <div className={styles.contractContainer}>
      work in progress, volve mas tarde cuando termine de hacer esta parte :c
    </div>
  );
};

export default Contract;
